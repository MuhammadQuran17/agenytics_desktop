---
description: Vue Inertia Rules
globs: *.vue, *.ts
alwaysApply: true
---

You are an expert in TypeScript, Vite, Vue.js v3.5, Inertia v2 and Tailwind v4, with a deep understanding of best practices and performance optimization techniques in these technologies. 

    What we use:
    - Inertia. So for api requests we can use useForm and .path, .post, e.t.c methods. Also we can use router.post, .patch, e.t.c methods.
    - Our Inertia and Laravel application is SSR.
    - Pinia. Notice we have store folder and store there state. In general, this folder is store for our frontend and we use Pinia.
    - reka-ui for ready to use UI components. In resources/js/components/ui/ folder we have components (card, button, dialog, sonner, button, e.t.c) on top of reka-ui components. Try to use them firstly and if there is no what we need, then you can install.
    - lucide-vue-next for icons 
    - tw-animate-css for animations
    - in resources/js/lib/utils.ts we have  function cn(...inputs: ClassValue[]) { return twMerge(clsx(inputs)); } for tailwind class merges

Notice: try to use these above mentioned tools. If you need some other 3rd party tools, firstly please ask before wrtiting and installing them to the code

    Code Style, Colors and Structure
    - Tailwind color variables are located in resources/css/app.css. Please use this these variables for coloring, fonts, radius, e.t.c. These variables represent our design tokens.
    - Write concise, maintainable, and technically accurate TypeScript code with relevant examples.
    - Use functional and declarative programming patterns; avoid classes.
    - Favor iteration and modularization to adhere to DRY principles and avoid code duplication.
    - Use descriptive variable names with auxiliary verbs (e.g., isLoading, hasError).
    - Organize files systematically: each file should contain only related content, such as exported components, subcomponents, helpers, static content, and types.
